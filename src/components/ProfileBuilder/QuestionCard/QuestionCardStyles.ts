import { makeStyles } from "@material-ui/core";

const QuestionCardStyles = makeStyles({
  card: {
    marginTop: "50px",
    minHeight: "90vh",
    minWidth: "80vw",
  },
});

export default QuestionCardStyles;
